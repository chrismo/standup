<nav class="navbar navbar-expand navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= settings_path %>"><i class="fas fa-cog"></i></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link <%= current_page?(standup_path) ? 'active' : '' %>" href="<%= standup_path %>">Standup</a>
      <%= link_to 'Logout', logout_path, class: "nav-item nav-link", data: { confirm: 'You sure you want to logout?' } %>
    </div>
  </div>
</nav>
